# aimade-mcp

## Commands

- `npm install` — install dependencies
- `npm run typecheck` — `tsc --noEmit`
- `npm test` — Vitest (`vitest run`)
- `npm run server` — the MCP server over stdio, in-memory store, no credentials
- `npm run demo` — walk the publish flow against the in-memory store
